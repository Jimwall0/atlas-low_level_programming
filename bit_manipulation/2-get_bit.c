#include "main.h"
/**
 * get_bit = grabs the bit at an index
 * @n: number being masked
 * @index: supposed index to grab bit
 * Return: bit
*/
int get_bit(unsigned long int n, unsigned int index)
{
	unsigned long int bit_mask = 1;

	for (; )
}